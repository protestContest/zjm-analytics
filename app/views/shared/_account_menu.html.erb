<div class='site-nav__item button-menu'>
  <div class='button-menu__button _mobile-hidden'>
    <div class='button-menu__title button-menu__title--small'>Account</div>
    <%= current_account.name %>
  </div>
  <div class='button-menu__button _mobile-only'>
    <%= image_tag('entypo/briefcase.svg', class: 'button-menu__icon') %>
  </div>
  <div class='button-menu__popup'>
    <div class='button-menu__menu' id='userMenu'>
      <% if current_user.all_accounts.size > 1 %>
        <div class='button-menu__title'>Switch account</div>
        <% current_user.all_accounts.each do |account| %>
          <%= link_to account.name, switch_account_path(account), class: 'button-menu__item' %>
        <% end %>
        <hr class='button-menu__divider' />
      <% end %>
      <%= link_to 'New account', new_user_account_path(current_user), class: 'button-menu__item' %>
    </div>
  </div>
</div>
