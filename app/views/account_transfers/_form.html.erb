<%= form_with(model: transfer, local: true, html: { class: 'form' }) do |form| %>
  <% if transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transfer.errors.count, "error") %> prohibited this account transfer from being saved:</h2>

      <ul>
      <% transfer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :account_id %>

  <div class="form__row">
    <%= form.label :target_owner, 'New owner email', class: 'label' %>
    <%= form.text_field :target_owner, class: 'input' %>
  </div>

  <div class="form__row">
    <%= form.submit 'Transfer account', class: 'button button--danger' %>
  </div>
<% end %>
